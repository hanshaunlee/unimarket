# Notebooks

This directory is intentionally empty. UniMarket's design assumes that all reproducible analyses run from configs, scripts, and the CLI. If you want exploratory notebooks, save them under `notebooks/` but do not rely on notebook execution for any reproducibility claim.
